namespace AonFreelancing.Enums
{
    /// <summary>
    /// this enum is intended only for seeding roles to the database
    /// </summary>
    public enum UserRoles
    {
        Freelancer,
        Client,
        SystemUser
    }
}
